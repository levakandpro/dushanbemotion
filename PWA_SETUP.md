# PWA Настройка - D MOTION

## ✅ Что было сделано:

### 1. Manifest (`public/site.webmanifest`)
- ✅ Обновлен с ориентацией `landscape`
- ✅ Добавлены все необходимые поля (`name`, `short_name`, `display: standalone`, `orientation`)
- ✅ Используются цвета бренда (`#0a0f1a`)
- ✅ Настроены иконки для всех необходимых размеров

### 2. Service Worker (`public/sw.js`)
- ✅ Базовое кэширование shell-части приложения (HTML, CSS, JS, иконки)
- ✅ Network-first стратегия для статических ресурсов
- ✅ НЕ кэширует динамические API запросы (`/api/`, `/r2-proxy/`, `/r2-people/`)
- ✅ Автоматическое обновление при новой версии SW

### 3. Регистрация Service Worker (`src/main.jsx`)
- ✅ Автоматическая регистрация при загрузке приложения
- ✅ Обработка обновлений SW

### 4. Фиксация ориентации (`src/utils/pwa.js`)
- ✅ Определение установленного PWA через `display-mode: standalone`
- ✅ Фиксация ориентации в `landscape` **ТОЛЬКО** для установленного PWA
- ✅ В обычном мобильном браузере ориентация НЕ фиксируется

### 5. HTML (`index.html`)
- ✅ Уже содержал ссылку на manifest
- ✅ Настроены theme-color и метатеги

### 6. Иконки
- ✅ Созданы временные иконки `icon-192x192.png` и `icon-512x512.png`
- ⚠️ Рекомендуется заменить на качественные версии из SVG (см. `PWA_ICONS_README.md`)

## 🎯 Поведение:

### Мобильный браузер (без PWA):
- Работает как обычно
- Показывается текущая заглушка для мобильных
- Ориентация НЕ фиксируется

### Установленное PWA (iOS/Android):
- Запускается как полноценное приложение
- Ориентация зафиксирована в `landscape` (горизонтально)
- Использует Service Worker для кэширования
- Работает в standalone режиме (без адресной строки браузера)

### Десктоп:
- Поведение НЕ изменилось
- PWA функциональность доступна, но ориентация не применяется (только для мобильных)

## 📱 Как установить PWA:

### iOS (Safari):
1. Откройте сайт в Safari
2. Нажмите кнопку "Поделиться"
3. Выберите "На экран «Домой»"
4. Добавьте приложение

### Android (Chrome):
1. Откройте сайт в Chrome
2. Нажмите меню (3 точки)
3. Выберите "Добавить на главный экран" или "Установить приложение"
4. Подтвердите установку

## 🔧 Технические детали:

### Определение PWA:
```javascript
// Функция isPWAInstalled() проверяет:
- window.matchMedia('(display-mode: standalone)').matches
- window.navigator.standalone (iOS)
- document.referrer.includes('android-app://')
```

### Фиксация ориентации:
- Использует `screen.orientation.lock('landscape')`
- Работает ТОЛЬКО в установленном PWA
- Не влияет на обычный браузер

### Service Worker:
- Кэширует только shell-файлы (статичные ресурсы)
- Не кэширует API запросы и динамический контент
- Автоматически обновляется при новой версии

## ⚠️ Важные замечания:

1. **iOS Safari ограничения:**
   - Screen Orientation API может не работать на iOS
   - Manifest `orientation: landscape` все равно укажет предпочтительную ориентацию
   - Некоторые функции могут работать по-разному

2. **Android Chrome:**
   - Полная поддержка Screen Orientation API
   - Ориентация будет зафиксирована в установленном PWA

3. **Иконки:**
   - Временно используются копии `apple-touch-icon.png`
   - Для продакшена рекомендуется создать качественные иконки из SVG

4. **Service Worker:**
   - При изменении SW, браузер обновит его автоматически
   - При необходимости принудительного обновления: очистите кэш в DevTools

## 🚀 Развертывание:

1. Убедитесь, что все файлы в `public/` копируются в `dist/` (Vite делает это автоматически)
2. Проверьте, что `sw.js` доступен по пути `/sw.js` после сборки
3. Проверьте, что `site.webmanifest` доступен по пути `/site.webmanifest`
4. Убедитесь, что сайт работает по HTTPS (PWA требует HTTPS)

## 🧪 Тестирование:

1. **Локально:**
   ```bash
   npm run dev
   ```
   - Откройте DevTools → Application → Service Workers
   - Проверьте регистрацию SW
   - Проверьте Manifest в разделе Manifest

2. **Production:**
   ```bash
   npm run build
   npm run preview
   ```
   - Используйте Chrome DevTools для проверки PWA
   - Проверьте установку на реальном устройстве

## 📝 Файлы:

- `public/site.webmanifest` - манифест PWA
- `public/sw.js` - service worker
- `src/main.jsx` - регистрация SW и фиксация ориентации
- `src/utils/pwa.js` - утилиты для работы с PWA
- `index.html` - HTML с ссылкой на manifest

