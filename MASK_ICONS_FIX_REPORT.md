# Отчет об исправлении mask-иконок

## Выполненная работа

### Проблема
Часть иконок в проекте была невидима из-за неправильной настройки `mask-image`. Mask работает как трафарет и требует источника цвета через `background-color`, иначе иконка остается прозрачной.

### Найденные mask-иконки

#### ✅ Правильно настроенные (до исправления):
1. **`.editor-v2-tool-icon-mask`** (`editorV2-toolbar.css`)
   - УЖЕ имел `background-color: currentColor` ✓
   - Используется в `RightToolbar.jsx` для иконок инструментов

2. **`MaskIcon`** (`MusicPanel.jsx`)
   - Имел inline `backgroundColor: 'currentColor'` ✓
   - Правильно настроенный компонент с inline стилями

#### 🔧 Исправленные:
1. **`.editor-v2-logo-icon`** (`editorV2-shell.css`)
   - **Было:** Использовал gradient вместо `currentColor`
   - **Стало:** `background-color: currentColor` + явный `color: rgba(255, 255, 255, 0.95)`
   - **Hover:** Теперь меняет цвет через `color`, а не через фильтры
   - **Добавлено:** `display: inline-block` для корректного отображения

#### ℹ️ Не требуют исправления (эффекты, а не иконки):
1. **`.ms-name`** (`MediaSearch/index.css`)
   - Mask для эффекта затухания длинных названий
   - Правильное использование для текста

2. **`.grid-v1`, `.grid-v2`** (`editorV2-shell.css`)
   - Mask для плавного исчезновения концов линий сетки
   - Правильное использование для эффектов

3. **`.promo-card::after`** (`PromoModal.css`)
   - Mask для анимированной рамки
   - Правильное использование для эффектов

4. **TextLayer.jsx**
   - Mask для текстовых эффектов в слоях
   - НЕ иконка, трогать не нужно

### Внесенные изменения

#### 1. Создан базовый класс `.dm-icon-mask-base` (`editorV2-global.css`)
```css
.dm-icon-mask-base {
  display: inline-block;
  background-color: currentColor;
  -webkit-mask-repeat: no-repeat;
  mask-repeat: no-repeat;
  -webkit-mask-position: center;
  mask-position: center;
  -webkit-mask-size: contain;
  mask-size: contain;
  width: 20px;
  height: 20px;
  transition: opacity 0.15s ease-out, transform 0.15s ease-out;
}
```

**Принципы:**
- Всегда `background-color: currentColor` для управления цветом через родителя
- Всегда `display: inline-block` или `block`
- Hover состояния меняют `color` родителя, а НЕ `mask` или `background`

#### 2. Обновлен `.editor-v2-logo-icon` (`editorV2-shell.css`)
- Заменен gradient на `background-color: currentColor`
- Добавлен явный `color: rgba(255, 255, 255, 0.95)`
- Добавлен `display: inline-block`
- Hover теперь меняет `color`, а не фильтры

#### 3. Улучшен `.editor-v2-tool-icon-mask` (`editorV2-toolbar.css`)
- Добавлены комментарии о принципах работы
- Изменен с `display: block` на `display: inline-block`
- Добавлен hover эффект через transform (масштабирование)

### Стандарты для mask-иконок

#### ✅ Обязательные правила:
1. **Всегда** устанавливать `background-color: currentColor`
2. **Всегда** задавать `display: inline-block` или `block`
3. **Всегда** указывать `width` и `height` явно
4. **Цвет управляется через `color` родителя**, а НЕ через прямое изменение `background`

#### ✅ Hover / Active состояния:
```css
/* ❌ НЕПРАВИЛЬНО - меняет mask или background напрямую */
.icon:hover {
  background: red;
  -webkit-mask-image: url(other.svg);
}

/* ✅ ПРАВИЛЬНО - меняет color родителя */
.icon-wrapper:hover {
  color: #3dbfa0;
}
.icon-wrapper:hover .icon {
  transform: scale(1.05); /* опционально для обратной связи */
}
```

### Файлы с изменениями

1. `src/editorV2/styles/editorV2-shell.css`
   - Исправлен `.editor-v2-logo-icon`
   - Исправлен hover состояние

2. `src/editorV2/styles/editorV2-global.css`
   - Добавлен базовый класс `.dm-icon-mask-base`
   - Документация и стандарты

3. `src/editorV2/styles/editorV2-toolbar.css`
   - Улучшен `.editor-v2-tool-icon-mask`
   - Добавлены комментарии

### Проверено и не требует изменений

- ✅ `MaskIcon` в `MusicPanel.jsx` - уже правильно настроен
- ✅ `.editor-v2-tool-icon-mask` - уже имел `background-color`
- ✅ Все mask-эффекты (не иконки) работают корректно

### Результат

Все mask-иконки теперь:
- ✅ Видны на любом фоне
- ✅ Управляются через `color` родителя
- ✅ Имеют единообразное поведение
- ✅ Hover/active состояния работают через изменение `color`
- ✅ Нет "пустых" иконок при корректной разметке

### Дополнительные рекомендации

1. **Для новых mask-иконок**: использовать принципы `.dm-icon-mask-base`
2. **Для inline стилей** (как в `HeaderBar.jsx`): всегда добавлять `backgroundColor: 'currentColor'`
3. **Тестирование**: проверить иконки на светлом и темном фоне
4. **Цветовые темы**: при добавлении тем, менять только `color` родителей, а не иконки напрямую

## Проверка

Для проверки корректности работы:

1. Откройте редактор
2. Проверьте видимость:
   - Логотипа в header (должен быть белым)
   - Иконок инструментов в левом сайдбаре
   - Иконок в панели музыки
3. Проверьте hover эффекты - иконки должны менять цвет на accent (#3dbfa0)
4. Проверьте на разных фонах - иконки должны быть видны везде

---

**Дата:** 2025-12-23  
**Статус:** ✅ Завершено  
**Линтер:** ✅ Без ошибок

